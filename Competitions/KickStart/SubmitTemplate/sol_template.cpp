#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array

// constraint, global definition


void solve() {
    //the 1st line of every case
	cin >> {para 1} >> {para 2} >> {para 3} ...;


    //logic
    
    
    // the output of # i 
	cout << dp[n][p] << "\n";
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int testNumbers, i=1;
    
    // 1st line: test number
	cin >> testNumbers;
	while(testNumbers--) {
		cout << "Case #" << i << ": ";
		solve();
		++i;
	}
}